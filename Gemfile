require 'rbconfig'

source 'https://rubygems.org'
ruby '2.0.0'
gem 'listen'
gem 'wdm', '>= 0.1.0' if RbConfig::CONFIG['target_os'] =~ /mswin|mingw/i

gem 'rake', :group => :test
gem 'mini-smtp-server', :group => :test
